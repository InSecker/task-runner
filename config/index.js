export const configuration = {
    base_url: 'https://jsonplaceholder.typicode.com',
    endpoints: {
        users: '/users',
        todos: '/todos',
        posts: '/posts',
        albums: '/albums'
    }
}
